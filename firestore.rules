rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All app data is read/written via Next.js Admin SDK.
    // Clients must not access Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
